#include <stdio.h>
#include <stdlib.h>

int main()
{
    add(stack, &tos, elem);
}
void add(int stack[], int *ptr, int elem)
{
    stack[++(*ptr)]=elem;
}
int pop(int stack[], int *ptr)
{
    return stck[(*ptr)--];

}
